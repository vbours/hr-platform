type Branch {
	branchId: ID!,
	address: String,
	city: String,
	country: String,
}

type Query {
	branches(count: Int):[Branch]
	branch(id: ID):Branch
}

type Mutation {
	createBranch(address: String!, city: String!, country: String):Branch
}